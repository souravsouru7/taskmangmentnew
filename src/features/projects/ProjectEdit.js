import React from 'react';
import { Container, Typography } from '@mui/material';

const ProjectEdit = () => {
  return (
    <Container maxWidth="lg" sx={{ mt: 4, mb: 4 }}>
      <Typography variant="h4" gutterBottom>
        Edit Project
      </Typography>
      {/* Project edit form will be added later */}
    </Container>
  );
};

export default ProjectEdit; 